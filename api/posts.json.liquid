---
---
[
    {% for post in site.posts %}
    {
        "title": {{ post.title | smartify | jsonify }},
        "intro": {{ post.intro | smartify | jsonify }},
        "link": {{ post.url | jsonify }},
        "date": "{{ post.date }}",
        "banner": {{ post.banner | jsonify }},
        "tags": {{ post.tags | jsonify }}
    } {% unless forloop.last %},{% endunless %}
    {% endfor %}
]
